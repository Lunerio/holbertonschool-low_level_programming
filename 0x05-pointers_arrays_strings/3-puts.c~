#include "holberton.h"

/**
 * main - check the code for Holberton School students.
 * @str: Pointer to str
 * Return: Always 0.
 */

void _puts(char *str)
{
	int index;

	for (index = 0; str[index] != '\n'; index++)
	{
		_putchar(str[index]);
	}
	_putchar('\n');
}
